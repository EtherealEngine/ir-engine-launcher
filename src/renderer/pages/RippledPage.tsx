import PageRoot from 'renderer/components/PageRoot'

import { Box } from '@mui/material'

const RippledPage = () => {
  return (
    <PageRoot full>
      <Box sx={{ display: 'flex', flexDirection: 'column', width: '100%' }}></Box>
    </PageRoot>
  )
}

export default RippledPage
